import { UniqueLibelleDirective } from './unique-libelle.directive';

describe('UniqueLibelleDirective', () => {
  it('should create an instance', () => {
    const directive = new UniqueLibelleDirective();
    expect(directive).toBeTruthy();
  });
});
